# Design.md — 菜谱合集设计规范

写给需要修改或新建本项目文件的 AI / 开发者。本文件讲的是**设计语言与视觉原则**——为什么长这样、该怎么保持一致。结构与代码规范见 [AGENT.md](AGENT.md)。

改动前请通读。核心一句话：**这是一本「印刷版食谱书」的数字化身，不是一个现代 App。** 任何改动都应让它更像一本用心排版的老菜谱书，而不是更像一个 SaaS 界面。

---

## 一、设计意图

整个站点在模仿**旧式印刷菜谱书 / 美式复古餐厅菜单**的质感：暖米色纸张、衬线大标题、朱红点缀、双线分隔、字母间距拉开的小标签。它应该看起来"被翻阅过、值得收藏"，而不是"刚从设计系统里生成"。

三个不变的支柱：

1. **纸感（Paper）** —— 背景永远是暖米色 `--paper`，配极淡的双色径向渐变，模拟纸张的不均匀。绝不用纯白 `#fff` 铺大面积背景（白色只用在标签文字、卡片半透明叠层上）。
2. **印刷排版（Print typography）** —— 标题、数字、引言一律用衬线体 `--serif`；正文、标签用无衬线 `--sans`。小标签统一大写 + 大字母间距（`letter-spacing`），像菜单上的栏目名。
3. **克制的暖色调（Restrained warm palette）** —— 朱红是唯一的主强调色，其余（橄榄、金、青绿）各有固定语义，不随意混用。

---

## 二、色彩系统

所有颜色都是 `:root` 里的 CSS 变量，**永远用变量，不要硬编码色值**。

| 变量 | 色值 | 角色 |
|---|---|---|
| `--paper` | `#f4ede0` | 主背景（暖米纸） |
| `--paper-deep` | `#ebe0cd` | 深一档纸色（步骤序号底、内嵌块） |
| `--ink` | `#2a2018` | 正文主色（近黑棕） |
| `--ink-soft` | `#5a4a38` | 次要文字、说明、引言 |
| `--rust` | `#a8341a` | **主强调**：标题 accent、返回链接、链接、hover 边框 |
| `--rust-deep` | `#7d2410` | 强调加深：数字、`<b>`、用量、计数 |
| `--olive` | `#5c6234` | 语义色：健身 / 可选项 / kicker |
| `--gold` | `#c08a1e` | 语义色：提示（tips）、变体标记、次强调 |
| `--teal` | `#2f6b66` | 语义色：备餐 / 保存信息 |
| `--line` | `#cbb89a` | 所有分隔线、边框、点线 |

**语义约定（重要，别打乱）：**

- **朱红 `--rust` = 主线**。菜名、序号、关键动作、导航都归它。全站只有这一个"主角色"。
- **橄榄 `--olive` = 健身视角**。健身卡片、`.lean` 徽章、index 上的 `fit` 标签、可选食材 `.opt`、kicker 都用它。
- **青绿 `--teal` = 备餐视角**。备餐卡片、保存时间数值、`prep` 标签。
- **金 `--gold` = 提示 / 补充 / 变体**。厨房备忘左边框、`.alt` 变体标记、bonus 提示。
- **红（`--rust`）在徽章里 = 警示/偏丰盛/不宜备餐**：`.lens-verdict.rich`、`.lens-verdict.low`、`.r-tag.prep.low`。

一句话记法：**主线红、健身绿、备餐青、提示金**。新增元素时先想"它属于哪条语义线"，再选对应变量。

---

## 三、字体与排版

```
--serif: "Hoefler Text", "Songti SC", "Source Han Serif SC", Georgia, "Times New Roman", serif;
--sans:  "Avenir Next", "PingFang SC", "Helvetica Neue", system-ui, sans-serif;
```

- **衬线 `--serif`**：大标题 `h1.title`、小节标题、菜名、所有"数字"（用量 `.qty`、温度 `.temp`、宏量 `.mv`、时间 `.r-time`、计数）、斜体引言 `.subtitle` / `.note` / `.lead`。数字用衬线是这套设计的签名细节，别改成无衬线。
- **无衬线 `--sans`**：正文、步骤、标签、徽章。
- **斜体引言**：`.subtitle`、`.note`、`.lead`、`.micro` 用 `font-style: italic` 的衬线，是"编者按"的语气，点到为止。
- **小标签大写间距**：`.eyebrow`、`.phase-tag`、`.r-kicker`、`.tag-pill`、徽章、`.meta-item .k` 全部 `text-transform: uppercase` + 较大 `letter-spacing`（0.1em–0.42em）。这是菜单栏目名的观感，是全站视觉节奏的关键。
- **响应式字号**：大标题用 `clamp()`，别写死 px。
- **绝不引入外部字体**（CSP 会拦截）。只用上面两个系统字体栈。

---

## 四、结构与间距原则

- **单栏纸张容器** `.sheet`：`max-width: 940px` 居中，两侧 `padding`。所有页面都在这个宽度内，像一张纸。
- **双线分隔** `3px double var(--line)`：hero 底、tips 顶、footer 顶。这是"章节分隔"的印刷手法，标志性元素，保留使用。
- **点线分隔** `1px dotted var(--line)`：列表项之间（食材、备餐速查）。像账单/清单的虚线。
- **详情页主体** `.body-grid`：左侧 `aside.ingredients`（sticky 粘顶，窄屏取消 sticky）+ 右侧 `main.steps`。
- **留白慷慨**：阶段之间 `margin-bottom: 46px`，小节之间大间距。宁可空，不要挤。
- **卡片半透明**：卡片/提示块用 `rgba(255,255,255,0.4~0.5)` 叠在纸色上，而非纯白，保留纸的暖底透出来。

---

## 五、组件速查（保持一致，别自创变体）

**每个页面的固定骨架**（class 名已由 `styles.css` 定义，照搬填内容）：

- `header.hero`：`.eyebrow` → `h1.title`（含 `.accent` 斜体红字）→ `.subtitle` → `.meta-strip`（若干 `.meta-item` > `.k`+`.v`）→ `.credit`
- `.body-grid`：左 `aside.ingredients`（`.ing-block` > `.ing-head` + `ul.ing-list`），右 `main.steps`（`.phase` > `.phase-tag` + `h2` + `.note` + `ol.step-list`）
- `section.tips`：`h2` + `.lead` + `.tip-grid`（`.tip`，金色左边框）
- `.lens-row`：`section.fitness`（橄榄底，💪）+ `section.prep`（青绿底，🥡）——**每道菜必备双视角**
- `footer.foot`：出处链接

**几个签名细节，务必延续：**

- **步骤序号**：`ol.step-list li::before` 是朱红描边的圆形数字，衬线体。不要换成方块或纯色填充。
- **阶段标签 `.phase-tag`**：按 `:nth-child` 循环配色（红→金→橄榄→墨→青），彩色小胶囊。第 6 个阶段会退回默认，尽量把步骤控制在 5 个阶段内，或接受循环。
- **用量 `.qty` / 温度 `.temp`**：右对齐、衬线、加粗、加深色。温度**同时标摄氏和华氏**。
- **徽章 `.lens-verdict`**：胶囊形，语义色（绿=friendly/lean，青=备餐正常，金=中性，红=偏丰盛rich/不宜备餐low）。
- **index 卡片**：`grid` 三栏（emoji | 内容 | 时间箭头），hover 上浮 + 朱红阴影 + 箭头右移。健身/备餐小标签颜色与详情页徽章语义一致。

---

## 六、双视角模块的语气

这是本项目的灵魂，措辞比样式更重要：

- **不要贴"能吃/不能吃""好/不好"的二元标签**。落在维度上：健身讲"**日常 vs 偶尔**"，备餐讲"**能否一次做、冷藏/冷冻、分批吃**"。
- 健身视角给宏量判断（有数据放 `.macro-row`，没有就定性说明，并用 `.micro` 注明"仅供参考"）。
- 备餐视角用 `.prep-facts` 列保存/复热/风味速查。
- 诚实：没有营养数据就说没有，别编。

---

## 七、写作与内容语气

- **全中文**，专有名词保留英文（Picanha、Gai Yang、Shawarma、Boeuf Bourguignon）。
- 引言/note/lead 用**斜体衬线的"编者按"口吻**——一句话点破关键，别啰嗦。
- 关键动作、温度、时长用 `<b>`（会自动变加深红）或 `.temp` 高亮。
- 页脚**必须保留原始出处链接**，不要去掉署名。
- 语气是"一个懂行的人在跟你分享私房菜"，温暖、有把握、不端着。

---

## 八、绝对不要做的事

- ❌ 引入外部字体 / CDN / JS 库 / 图标库（CSP 会静默拦截，页面会坏）。少量交互用内联原生 JS。
- ❌ 硬编码颜色值——一律用 CSS 变量。
- ❌ 用纯白铺大面积背景，或改成冷色调/现代扁平风。
- ❌ 在菜谱页内嵌 `<style>`（除极个别页面专属样式，且需在 `styles.css` 里加注释说明用途，如 `.alt*` / `.bonus*` 仅勃艮第炖牛肉用）。
- ❌ 把衬线数字改成无衬线、把圆形序号改成方块、去掉双线分隔——这些是识别度所在。
- ❌ 打乱色彩语义（比如用青绿去标健身、用红去做普通装饰）。
- ❌ 省掉底部双视角模块或页脚出处。

---

## 九、自查清单

改完后对照：

- [ ] 只用了 `:root` 里的颜色变量，没有硬编码
- [ ] 标题/数字是衬线，正文/标签是无衬线
- [ ] 小标签是大写 + 大字母间距
- [ ] 背景是纸色（非纯白），卡片是半透明叠层
- [ ] 色彩语义正确：主线红 / 健身绿 / 备餐青 / 提示金
- [ ] 保留了双线分隔、圆形序号、彩色阶段标签
- [ ] 温度双单位、用量右对齐衬线
- [ ] 双视角模块在，措辞落在"日常 vs 偶尔 / 能否备餐"
- [ ] 没有引入任何外部依赖
- [ ] 整体更像一本印刷菜谱书，而不是一个 App
